# Database URL - defaults to development database
# Override with DATABASE_URL for different environments
DATABASE_URL=./data/dev/local.db

# Better Auth Secret - change in production!
BETTER_AUTH_SECRET=dev-secret-change-in-production
